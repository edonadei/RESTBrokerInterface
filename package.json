{
  "name": "brokerexecute",
  "version": "1.0.0",
  "description": "A package to generify execution of batches delivered by message brokers",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.88.0"
  }
}
